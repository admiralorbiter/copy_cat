# Antigravity Architecture Plan

Here is the proposed architecture plan for the application.

> [!NOTE]
> Ensure all unit tests maintain high coverage.

## Core Features

Use `TextPattern` to inspect UI controls.

```python
def extract_text(element):
    return element.get_text()
```

See [the documentation](https://example.com/docs) for full details.
